#include<stdio.h>
int main(){
    int l, b, a, p;
    l = 30;
    b = 40;
    p = 2 * (l + b);
    printf("The perimeter of the rectangle is: %d\n", p);
    a = l * b;
    printf("The area of the rectangle is: %d", a);
    return 0; 
}